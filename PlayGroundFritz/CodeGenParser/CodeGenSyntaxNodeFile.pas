namespace ProHolz.CodeGen;

interface
uses ProHolz.Ast;
// Should be removed after we have all Set Types
type
  CodeBuilderMethods = static partial class
    private
     // method getRawNodeData(const node : TSyntaxNode) : String;
    public
    //  property FileData : List<String>;
  end;
implementation

//method CodeBuilderMethods.getRawNodeData(const node: TSyntaxNode): String;
//begin
 //if (FileData <> nil) and (node is TCompoundSyntaxNode) then
  //begin
    //Var cnode := TCompoundSyntaxNode(node);
    //for  i : Integer := cnode.Line to cnode.EndLine do
      //result := result+FileData[i-1];
  //end;
//end;

end.